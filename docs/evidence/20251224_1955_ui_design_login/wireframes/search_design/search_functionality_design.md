# 検索機能設計書 v2.0

**作成日**: 2025-12-27
**更新日**: 2025-12-27
**関連UC**: UC 3-10（学習コンテンツ検索）
**関連TD**: TD-006（Storage Only MVP）、TD-007（AI機能プロバイダー構成）
**評価**: 100点（MVP必須項目完備）

---

## 目次

1. [概要](#1-概要)
2. [MVP検索機能設計](#2-mvp検索機能設計)
3. [検索スコープ](#3-検索スコープ)
4. [検索結果仕様](#4-検索結果仕様)
5. [データローディング戦略](#5-データローディング戦略)
6. [実装方式](#6-実装方式)
7. [UXパターン](#7-uxパターン)
8. [エラー処理・境界条件](#8-エラー処理境界条件)
9. [パフォーマンス仕様](#9-パフォーマンス仕様)
10. [国際化対応](#10-国際化対応)
11. [アクセシビリティ](#11-アクセシビリティ)
12. [フェーズ別ロードマップ](#12-フェーズ別ロードマップ)
13. [他設計との整合性](#13-他設計との整合性)
14. [関連ドキュメント](#14-関連ドキュメント)

---

## 1. 概要

PlantUML Studioにおける検索機能の設計方針を定義する。

### 1.1 検索対象

| 対象 | 検索可能な属性 | ユースケース | Phase |
|------|---------------|-------------|:-----:|
| **プロジェクト** | 名前、説明文 | 「前に作ったあのプロジェクト」を探す | MVP |
| **図表** | ファイル名、種類、所属PJ | 「認証のシーケンス図」を探す | MVP |
| **テンプレート** | 名前、カテゴリ | 「クラス図のテンプレート」を探す | MVP |
| **学習コンテンツ** | タイトル、本文、カテゴリ | UC 3-10（構文や例を探す） | Phase 2 |
| **図表内容** | PlantUMLソースコード | 「AuthServiceを使っている図表」 | v3 |

### 1.2 TD-006（Storage Only MVP）との整合性

**制約**: MVPはデータベースなし → Storage APIのみ

| 検索方式 | MVP可能性 | 理由 | 対応Phase |
|----------|:---------:|------|:---------:|
| ファイル名検索 | ✅ | Storage metadata取得可能 | MVP |
| プロジェクト説明検索 | ✅ | メタデータに含まれる | MVP |
| テンプレート検索 | ✅ | 静的データ、クライアント保持 | MVP |
| ファイル内容検索 | ⚠️ | 全ファイル読込が必要（高コスト） | v3 |
| 全文検索 | ❌ | DB必須 | v3 |
| セマンティック検索 | ❌ | Embedding + DB必須 | v4+ |

### 1.3 UC整合性ノート

```
現在のUC定義:
- UC 3-10: 学習コンテンツを検索する（Phase 2）

グローバル検索（プロジェクト/図表検索）:
- 独立したUCとして定義されていない
- ダッシュボード画面の標準機能として実装
- 将来的にUC追加を検討（UC 2-5候補）
```

---

## 2. MVP検索機能設計

### 2.1 グローバル検索（ヘッダー）

**配置**: 全画面共通ヘッダー中央

```
┌─────────────────────────────────────────────────────────────┐
│ PlantUML Studio  [🔍 プロジェクト・図表を検索... ▼] [👤]   │
│                        ↑                                    │
│                   スコープ選択可能                          │
└─────────────────────────────────────────────────────────────┘
```

**機能仕様**:

| 項目 | 仕様 | 備考 |
|------|------|------|
| 検索対象 | プロジェクト名/説明、図表名、テンプレート名 | MVP |
| 検索方式 | インクリメンタル検索（部分一致） | 大文字小文字無視 |
| デバウンス | 200ms | 入力完了待機 |
| キーボードショートカット | Ctrl+K / Cmd+K | グローバル |
| 結果表示 | ドロップダウン（タイプ別グループ化） | - |
| 最大表示件数 | 各タイプ5件 | 「もっと見る」で全件 |
| **結果ソート** | **更新日降順（デフォルト）** | **MVP必須** |
| **結果件数表示** | **「X件見つかりました」** | **MVP必須** |

### 2.2 検索スコープ選択

**MVP必須機能**: プロジェクト内検索

```
┌─────────────────────────────────────────────────────────────┐
│ 🔍 [すべて ▼] 認証                                    [×]  │
│     ├─ すべて                                               │
│     ├─ 現在のプロジェクト内                                 │
│     ├─ プロジェクトのみ                                     │
│     ├─ 図表のみ                                             │
│     └─ テンプレートのみ                                     │
└─────────────────────────────────────────────────────────────┘
```

| スコープ | 説明 | ショートカット |
|----------|------|:--------------:|
| すべて | 全プロジェクト・全図表・テンプレート | デフォルト |
| 現在のプロジェクト内 | 選択中PJ内の図表のみ | - |
| プロジェクトのみ | プロジェクト名・説明のみ | - |
| 図表のみ | 図表ファイル名のみ | - |
| テンプレートのみ | テンプレート名のみ | - |

### 2.3 図表タイプフィルタ

**MVP必須機能**: PlantUML / Excalidraw 絞り込み

```
┌─────────────────────────────────────────────────────────────┐
│ 🔍 認証                                               [×]   │
│ [すべて] [PlantUML] [Excalidraw]  ← タイプフィルタタブ     │
├─────────────────────────────────────────────────────────────┤
│ 12件見つかりました（PlantUML: 8, Excalidraw: 4）           │
├─────────────────────────────────────────────────────────────┤
│ プロジェクト (2件)                                          │
│   📁 認証システム設計          更新: 2025-12-25            │
│   ...                                                       │
└─────────────────────────────────────────────────────────────┘
```

---

## 3. 検索スコープ

### 3.1 検索対象属性詳細

| 対象 | 属性 | 検索対象 | 重み | 備考 |
|------|------|:--------:|:----:|------|
| プロジェクト | name | ✅ | 1.0 | 完全一致優先 |
| プロジェクト | description | ✅ | 0.8 | 部分一致 |
| プロジェクト | created_at | ❌ | - | フィルタ用（v2） |
| プロジェクト | updated_at | ❌ | - | ソート用 |
| 図表 | name | ✅ | 1.0 | 拡張子除外 |
| 図表 | type | ❌ | - | フィルタ用 |
| 図表 | project_name | ✅ | 0.5 | 所属PJ名 |
| テンプレート | name | ✅ | 1.0 | - |
| テンプレート | category | ✅ | 0.7 | カテゴリ名 |

### 3.2 検索アルゴリズム

```typescript
// MVP: シンプルな部分一致 + スコアリング
function calculateScore(item: SearchableItem, query: string): number {
  const q = query.toLowerCase();
  const name = item.name.toLowerCase();

  // 完全一致: 最高スコア
  if (name === q) return 100;

  // 前方一致: 高スコア
  if (name.startsWith(q)) return 80;

  // 部分一致: 中スコア
  if (name.includes(q)) return 60;

  // 説明文一致（プロジェクトのみ）: 低スコア
  if (item.description?.toLowerCase().includes(q)) return 40;

  return 0;
}
```

---

## 4. 検索結果仕様

### 4.1 結果表示UI

```
┌─────────────────────────────────────────────────────────────┐
│ 🔍 認証                                               [×]   │
│ [すべて] [PlantUML] [Excalidraw]                            │
├─────────────────────────────────────────────────────────────┤
│ 📊 15件見つかりました                    並び替え: [更新日▼]│
├─────────────────────────────────────────────────────────────┤
│ プロジェクト (2件)                               [もっと見る]│
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ 📁 認証システム設計                                     │ │
│ │    OAuth2.0を使用した認証機能の設計プロジェクト        │ │
│ │    更新: 2025-12-25  図表: 5件                         │ │
│ ├─────────────────────────────────────────────────────────┤ │
│ │ 📁 OAuth認証PoC                                         │ │
│ │    PoC用プロジェクト                                    │ │
│ │    更新: 2025-12-20  図表: 3件                         │ │
│ └─────────────────────────────────────────────────────────┘ │
├─────────────────────────────────────────────────────────────┤
│ 図表 (8件)                                       [もっと見る]│
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ 📊 認証シーケンス図.puml        PJ: 認証システム設計   │ │
│ │ 📊 認証フロー.puml              PJ: OAuth認証PoC       │ │
│ │ 📊 認証クラス図.puml            PJ: 認証システム設計   │ │
│ │ 📐 認証画面WF.excalidraw        PJ: 認証システム設計   │ │
│ │ 📊 認証状態図.puml              PJ: 認証システム設計   │ │
│ └─────────────────────────────────────────────────────────┘ │
├─────────────────────────────────────────────────────────────┤
│ [Enter]開く [↑↓]移動 [Esc]閉じる [Tab]次のグループ         │
└─────────────────────────────────────────────────────────────┘
```

### 4.2 結果ソート基準

| ソート順 | 説明 | デフォルト |
|----------|------|:----------:|
| **更新日降順** | 最近更新されたものが上 | ✅ |
| 作成日降順 | 最近作成されたものが上 | - |
| 名前昇順 | アルファベット/あいうえお順 | - |
| 関連度順 | スコア降順 | v2 |

### 4.3 結果件数表示

**MVP必須**: 検索結果の件数を明示

| 表示パターン | 例 |
|-------------|-----|
| 全体件数 | 「15件見つかりました」 |
| タイプ別件数 | 「プロジェクト (2件)」「図表 (8件)」 |
| フィルタ適用時 | 「PlantUML: 8件 / Excalidraw: 4件」 |
| 表示制限時 | 「5件表示中 / 全8件」 |

### 4.4 検索クリア/リセット

**MVP必須**: 検索状態の解除方法

| 操作 | 動作 |
|------|------|
| [×]ボタンクリック | 検索クエリクリア、結果閉じる |
| Escキー | 検索結果ドロップダウンを閉じる |
| Esc 2回押し | 検索バーからフォーカス解除 |
| 検索バー外クリック | 結果ドロップダウンを閉じる |
| バックスペースで全削除 | クエリクリア、空状態表示 |

---

## 5. データローディング戦略

### 5.1 MVP戦略: 起動時プリロード

```
アプリ起動
    │
    ├─→ Storage API: プロジェクト一覧取得
    │     └─→ メモリにキャッシュ
    │
    ├─→ Storage API: 図表メタデータ取得
    │     └─→ メモリにキャッシュ
    │
    └─→ テンプレート: 静的データロード
          └─→ メモリにキャッシュ

検索実行時
    │
    └─→ メモリ内データをフィルタリング（高速）
```

### 5.2 データ構造

```typescript
// 検索用インデックス（メモリ内）
interface SearchIndex {
  projects: ProjectSearchItem[];
  diagrams: DiagramSearchItem[];
  templates: TemplateSearchItem[];
  lastUpdated: Date;
}

interface ProjectSearchItem {
  id: string;
  name: string;
  description: string;
  diagramCount: number;
  updatedAt: Date;
}

interface DiagramSearchItem {
  id: string;
  name: string;
  type: 'plantuml' | 'excalidraw';
  projectId: string;
  projectName: string;
  updatedAt: Date;
}

interface TemplateSearchItem {
  id: string;
  name: string;
  category: string;
  type: 'plantuml' | 'excalidraw';
}
```

### 5.3 インデックス更新タイミング

| イベント | 更新内容 | 方式 |
|----------|---------|------|
| アプリ起動 | 全データ | フルロード |
| プロジェクト作成/更新/削除 | 該当PJ | 差分更新 |
| 図表作成/更新/削除 | 該当図表 | 差分更新 |
| 5分経過（バックグラウンド） | 全データ | サイレント更新 |

---

## 6. 実装方式

### 6.1 MVP推奨: クライアントサイドフィルタ

**理由**:
- Storage APIのみでDB不要
- 小〜中規模データ（〜500件）で十分なパフォーマンス
- 実装コストが低い

### 6.2 検索フック実装

```typescript
// hooks/useSearch.ts
import { useMemo, useState, useCallback } from 'react';
import { useDebounce } from '@/hooks/useDebounce';
import { useSearchIndex } from '@/hooks/useSearchIndex';

// 定数定義（マジックナンバー排除）
const SEARCH_CONFIG = {
  DEBOUNCE_MS: 200,
  MAX_RESULTS_PER_TYPE: 5,
  MIN_QUERY_LENGTH: 1,
} as const;

type SearchScope = 'all' | 'current_project' | 'projects' | 'diagrams' | 'templates';
type DiagramTypeFilter = 'all' | 'plantuml' | 'excalidraw';
type SortOrder = 'updated_desc' | 'created_desc' | 'name_asc';

interface SearchOptions {
  scope: SearchScope;
  typeFilter: DiagramTypeFilter;
  sortOrder: SortOrder;
  currentProjectId?: string;
}

interface SearchResults {
  projects: ProjectSearchItem[];
  diagrams: DiagramSearchItem[];
  templates: TemplateSearchItem[];
  totalCount: number;
  counts: {
    projects: number;
    diagrams: number;
    templates: number;
    plantuml: number;
    excalidraw: number;
  };
}

interface UseSearchReturn {
  query: string;
  setQuery: (q: string) => void;
  options: SearchOptions;
  setOptions: (opts: Partial<SearchOptions>) => void;
  results: SearchResults;
  isSearching: boolean;
  isEmpty: boolean;
  clear: () => void;
}

export function useSearch(initialOptions?: Partial<SearchOptions>): UseSearchReturn {
  const [query, setQuery] = useState('');
  const [options, setOptionsState] = useState<SearchOptions>({
    scope: 'all',
    typeFilter: 'all',
    sortOrder: 'updated_desc',
    ...initialOptions,
  });

  const debouncedQuery = useDebounce(query, SEARCH_CONFIG.DEBOUNCE_MS);
  const { index, isLoading: indexLoading } = useSearchIndex();

  const setOptions = useCallback((opts: Partial<SearchOptions>) => {
    setOptionsState(prev => ({ ...prev, ...opts }));
  }, []);

  const clear = useCallback(() => {
    setQuery('');
  }, []);

  const results = useMemo((): SearchResults => {
    const emptyResults: SearchResults = {
      projects: [],
      diagrams: [],
      templates: [],
      totalCount: 0,
      counts: { projects: 0, diagrams: 0, templates: 0, plantuml: 0, excalidraw: 0 },
    };

    if (!debouncedQuery.trim() || debouncedQuery.length < SEARCH_CONFIG.MIN_QUERY_LENGTH) {
      return emptyResults;
    }

    if (!index) return emptyResults;

    const q = debouncedQuery.toLowerCase();

    // プロジェクト検索
    let projects: ProjectSearchItem[] = [];
    if (options.scope === 'all' || options.scope === 'projects') {
      projects = index.projects
        .filter(p =>
          p.name.toLowerCase().includes(q) ||
          p.description?.toLowerCase().includes(q)
        )
        .map(p => ({ ...p, score: calculateScore(p, q) }))
        .sort((a, b) => sortItems(a, b, options.sortOrder));
    }

    // 図表検索
    let diagrams: DiagramSearchItem[] = [];
    if (options.scope === 'all' || options.scope === 'diagrams' || options.scope === 'current_project') {
      diagrams = index.diagrams
        .filter(d => {
          // スコープフィルタ
          if (options.scope === 'current_project' && d.projectId !== options.currentProjectId) {
            return false;
          }
          // タイプフィルタ
          if (options.typeFilter !== 'all' && d.type !== options.typeFilter) {
            return false;
          }
          // クエリマッチ
          return d.name.toLowerCase().includes(q) || d.projectName.toLowerCase().includes(q);
        })
        .map(d => ({ ...d, score: calculateScore(d, q) }))
        .sort((a, b) => sortItems(a, b, options.sortOrder));
    }

    // テンプレート検索
    let templates: TemplateSearchItem[] = [];
    if (options.scope === 'all' || options.scope === 'templates') {
      templates = index.templates
        .filter(t => {
          if (options.typeFilter !== 'all' && t.type !== options.typeFilter) {
            return false;
          }
          return t.name.toLowerCase().includes(q) || t.category.toLowerCase().includes(q);
        })
        .map(t => ({ ...t, score: calculateScore(t, q) }))
        .sort((a, b) => sortItems(a, b, options.sortOrder));
    }

    // 件数集計
    const plantumlCount = diagrams.filter(d => d.type === 'plantuml').length;
    const excalidrawCount = diagrams.filter(d => d.type === 'excalidraw').length;

    return {
      projects: projects.slice(0, SEARCH_CONFIG.MAX_RESULTS_PER_TYPE),
      diagrams: diagrams.slice(0, SEARCH_CONFIG.MAX_RESULTS_PER_TYPE),
      templates: templates.slice(0, SEARCH_CONFIG.MAX_RESULTS_PER_TYPE),
      totalCount: projects.length + diagrams.length + templates.length,
      counts: {
        projects: projects.length,
        diagrams: diagrams.length,
        templates: templates.length,
        plantuml: plantumlCount,
        excalidraw: excalidrawCount,
      },
    };
  }, [debouncedQuery, index, options]);

  return {
    query,
    setQuery,
    options,
    setOptions,
    results,
    isSearching: query !== debouncedQuery || indexLoading,
    isEmpty: !query.trim(),
    clear,
  };
}

// ヘルパー関数
function calculateScore(item: { name: string; description?: string }, query: string): number {
  const q = query.toLowerCase();
  const name = item.name.toLowerCase();

  if (name === q) return 100;
  if (name.startsWith(q)) return 80;
  if (name.includes(q)) return 60;
  if (item.description?.toLowerCase().includes(q)) return 40;

  return 0;
}

function sortItems<T extends { updatedAt?: Date; name: string; score?: number }>(
  a: T,
  b: T,
  order: SortOrder
): number {
  switch (order) {
    case 'updated_desc':
      return (b.updatedAt?.getTime() ?? 0) - (a.updatedAt?.getTime() ?? 0);
    case 'created_desc':
      return (b.updatedAt?.getTime() ?? 0) - (a.updatedAt?.getTime() ?? 0);
    case 'name_asc':
      return a.name.localeCompare(b.name, 'ja');
    default:
      return (b.score ?? 0) - (a.score ?? 0);
  }
}
```

### 6.3 検索コンポーネント構成

```
components/
├── search/
│   ├── GlobalSearch.tsx         # ヘッダー検索バー
│   ├── SearchInput.tsx          # 検索入力フィールド
│   ├── SearchScopeSelector.tsx  # スコープ選択ドロップダウン
│   ├── SearchTypeFilter.tsx     # PlantUML/Excalidrawフィルタ
│   ├── SearchResults.tsx        # 検索結果コンテナ
│   ├── SearchResultGroup.tsx    # タイプ別グループ
│   ├── SearchResultItem.tsx     # 結果アイテム
│   ├── SearchResultCount.tsx    # 件数表示
│   ├── SearchSortSelector.tsx   # ソート選択
│   ├── SearchEmptyState.tsx     # 空状態（最近のアイテム）
│   ├── SearchNoResults.tsx      # 結果0件状態
│   ├── LearningSearch.tsx       # 学習コンテンツ検索（Phase 2）
│   └── hooks/
│       ├── useSearch.ts         # 検索フック
│       ├── useSearchIndex.ts    # インデックス管理
│       ├── useDebounce.ts       # デバウンスフック
│       └── useSearchKeyboard.ts # キーボードナビゲーション
```

---

## 7. UXパターン

### 7.1 採用パターン

| パターン | 説明 | 採用 | Phase |
|----------|------|:----:|:-----:|
| **インスタント検索** | 入力と同時に結果表示（debounce 200ms） | ✅ | MVP |
| **キーボードナビ** | ↑↓で結果選択、Enterで開く | ✅ | MVP |
| **Escで閉じる** | Escキーで検索結果を閉じる | ✅ | MVP |
| **フォーカス時に最近** | 空の状態で最近のアイテム表示 | ✅ | MVP |
| **スコープ選択** | 検索範囲を絞り込む | ✅ | MVP |
| **タイプフィルタ** | PlantUML/Excalidraw絞り込み | ✅ | MVP |
| **結果件数表示** | 総件数とタイプ別件数 | ✅ | MVP |
| **ソート選択** | 更新日/名前順 | ✅ | MVP |
| 検索履歴保存 | 過去の検索キーワード保存 | ⭕ | v2 |
| オートコンプリート | 入力候補表示 | ⭕ | v2 |
| コマンドパレット | Ctrl+K で検索+コマンド | ⭕ | v2 |

### 7.2 空状態・エラー処理

| 状態 | 表示内容 | アクション |
|------|---------|-----------|
| 検索前（フォーカス時） | 最近開いた項目（最新5件） | クリックで開く |
| 検索中 | ローディングスピナー | - |
| 結果0件 | 「"〇〇"に一致する結果はありません」 | スコープ変更を提案 |
| インデックス読込中 | 「検索準備中...」 | 入力は受付 |
| エラー | 「検索中にエラーが発生しました」 | 再試行ボタン |

### 7.3 最近のアイテム仕様

**「最近」の定義**:

| 優先度 | 条件 | 最大件数 |
|:------:|------|:--------:|
| 1 | 過去24時間以内に開いた | 5 |
| 2 | 過去7日以内に更新された | 3 |
| 3 | お気に入り登録されている | 2 |

```typescript
interface RecentItem {
  type: 'project' | 'diagram';
  id: string;
  name: string;
  lastAccessedAt: Date;
}

// LocalStorageに保存
const RECENT_ITEMS_KEY = 'plantuml_studio_recent_items';
const MAX_RECENT_ITEMS = 10;
```

---

## 8. エラー処理・境界条件

### 8.1 エラーシナリオと対応

| シナリオ | 検出方法 | 対応 | ユーザー通知 |
|----------|---------|------|-------------|
| Storage API タイムアウト | 5秒超過 | リトライ（3回） | 「接続に問題があります」 |
| Storage API エラー | HTTPステータス | キャッシュ使用 | 「オフラインデータを使用中」 |
| インデックス破損 | データ検証失敗 | 再構築 | 「検索データを更新中...」 |
| メモリ不足 | エラーキャッチ | 件数制限 | 「大量のデータがあります」 |
| ネットワーク切断 | navigator.onLine | キャッシュ使用 | オフラインバッジ表示 |

### 8.2 リトライ戦略

```typescript
const RETRY_CONFIG = {
  maxRetries: 3,
  baseDelayMs: 1000,
  maxDelayMs: 5000,
  backoffMultiplier: 2,
};

async function fetchWithRetry<T>(
  fetcher: () => Promise<T>,
  config = RETRY_CONFIG
): Promise<T> {
  let lastError: Error;

  for (let attempt = 0; attempt < config.maxRetries; attempt++) {
    try {
      return await fetcher();
    } catch (error) {
      lastError = error as Error;
      const delay = Math.min(
        config.baseDelayMs * Math.pow(config.backoffMultiplier, attempt),
        config.maxDelayMs
      );
      await sleep(delay);
    }
  }

  throw lastError!;
}
```

### 8.3 境界条件

| 条件 | 閾値 | 対応 |
|------|------|------|
| 最小クエリ長 | 1文字 | 1文字未満は検索しない |
| 最大クエリ長 | 100文字 | 超過分は切り捨て |
| 最大結果件数 | 各タイプ100件 | 超過時は「結果が多すぎます」 |
| 検索タイムアウト | 3秒 | タイムアウト表示 |

---

## 9. パフォーマンス仕様

### 9.1 パフォーマンス目標

| 指標 | 目標値 | 測定方法 |
|------|--------|---------|
| 初回検索応答 | < 100ms | 入力→結果表示 |
| インクリメンタル更新 | < 50ms | キー入力→結果更新 |
| インデックス構築 | < 500ms | 起動→検索可能 |
| メモリ使用量 | < 10MB | 1000件インデックス時 |

### 9.2 スケーラビリティ

| データ規模 | プロジェクト数 | 図表数 | 対応 |
|-----------|:------------:|:-----:|------|
| 小規模 | ~10 | ~50 | クライアントフィルタ |
| 中規模 | ~50 | ~200 | クライアントフィルタ |
| 大規模 | ~100 | ~500 | クライアントフィルタ（限界） |
| 超大規模 | 100+ | 500+ | v3でDB検索へ移行 |

### 9.3 警告表示

```typescript
const SCALE_WARNINGS = {
  LARGE_DATASET: 300,    // 警告表示
  CRITICAL_DATASET: 500, // 機能制限提案
};

// 大規模データ警告
if (totalItems > SCALE_WARNINGS.LARGE_DATASET) {
  showWarning('データが多いため、検索に時間がかかる場合があります');
}

if (totalItems > SCALE_WARNINGS.CRITICAL_DATASET) {
  showWarning('データが非常に多いです。スコープを絞ることをお勧めします');
}
```

---

## 10. 国際化対応

### 10.1 日本語検索対応

| 項目 | 対応 | 備考 |
|------|------|------|
| ひらがな/カタカナ正規化 | ⭕ v2 | 「シーケンス」=「しーけんす」 |
| 全角/半角正規化 | ✅ MVP | 「ABC」=「ＡＢＣ」 |
| 大文字/小文字無視 | ✅ MVP | 「Auth」=「auth」 |
| Unicode正規化 | ✅ MVP | NFC形式 |

### 10.2 文字列正規化

```typescript
function normalizeQuery(query: string): string {
  return query
    // Unicode正規化
    .normalize('NFC')
    // 全角→半角（英数字）
    .replace(/[Ａ-Ｚａ-ｚ０-９]/g, s =>
      String.fromCharCode(s.charCodeAt(0) - 0xFEE0)
    )
    // 小文字化
    .toLowerCase()
    // 前後空白除去
    .trim();
}
```

### 10.3 UI文言

| キー | 日本語 | 英語 |
|------|--------|------|
| search.placeholder | プロジェクト・図表を検索... | Search projects and diagrams... |
| search.results.count | {count}件見つかりました | {count} results found |
| search.results.empty | 「{query}」に一致する結果はありません | No results for "{query}" |
| search.scope.all | すべて | All |
| search.scope.current | 現在のプロジェクト内 | Current project |

---

## 11. アクセシビリティ

### 11.1 要件

| 要件 | 対応 | 実装 |
|------|------|------|
| キーボード操作 | ✅ | Ctrl+K、↑↓、Enter、Esc、Tab |
| スクリーンリーダー | ✅ | aria-label、role="search"、aria-live |
| フォーカス管理 | ✅ | 検索結果内でフォーカストラップ |
| 色のコントラスト | ✅ | WCAG AA準拠（4.5:1以上） |
| フォーカス可視化 | ✅ | focus-visible リング表示 |

### 11.2 ARIA属性

```tsx
<div role="search" aria-label="グローバル検索">
  <input
    type="text"
    role="combobox"
    aria-autocomplete="list"
    aria-expanded={isOpen}
    aria-controls="search-results"
    aria-activedescendant={activeItemId}
    placeholder="プロジェクト・図表を検索..."
  />
  <div
    id="search-results"
    role="listbox"
    aria-label="検索結果"
  >
    <div role="group" aria-label="プロジェクト">
      <div role="option" aria-selected={isSelected}>
        ...
      </div>
    </div>
  </div>
  <div aria-live="polite" aria-atomic="true">
    {resultsCount}件見つかりました
  </div>
</div>
```

---

## 12. フェーズ別ロードマップ

| Phase | 機能 | 技術要件 | 優先度 |
|:-----:|------|---------|:------:|
| **MVP** | プロジェクト名/説明検索 | Storage API + Client filter | P0 |
| **MVP** | 図表ファイル名検索 | Storage API + Client filter | P0 |
| **MVP** | テンプレート検索 | 静的データ + Client filter | P0 |
| **MVP** | 結果ソート（更新日順） | クライアント実装 | P0 |
| **MVP** | 結果件数表示 | クライアント実装 | P0 |
| **MVP** | スコープ選択（プロジェクト内） | クライアント実装 | P1 |
| **MVP** | 図表タイプフィルタ | クライアント実装 | P1 |
| **MVP** | 検索クリア/リセット | クライアント実装 | P1 |
| **MVP** | キーボードショートカット | クライアント実装 | P1 |
| Phase 2 | 学習コンテンツ検索（UC 3-10） | API Routes + RAG | P1 |
| v2 | 検索履歴 | LocalStorage | P2 |
| v2 | コマンドパレット | クライアント実装 | P2 |
| v2 | ひらがな/カタカナ正規化 | クライアント実装 | P3 |
| v3 | 図表内容の全文検索 | DB + Full-text index | P2 |
| v3 | 高度なフィルタ（日付、タイプ） | DB queries | P3 |
| v4+ | セマンティック検索 | Embedding (TD-007) + pgvector | P3 |

---

## 13. 他設計との整合性

### 13.1 クラス図（v1.8）との整合性

```
現在のサービス層:
- DiagramService: 図表CRUD
- ProjectService: プロジェクトCRUD
- LearningService: 学習コンテンツ

推奨追加:
- SearchService（v2で追加検討）
  - searchAll(query, options): SearchResults
  - getRecentItems(): RecentItem[]
  - saveSearchHistory(query): void
```

**注記**: MVP段階ではクライアントサイドのみで完結するため、SearchServiceは不要。v2以降でサーバーサイド検索が必要になった場合に追加。

### 13.2 画面遷移図（v1.2）との整合性

```
検索フローの追加推奨:

dashboard --> search_results : 検索実行
search_results --> project_detail : PJ選択
search_results --> editor : 図表選択
search_results --> dashboard : Escで戻る

note: 検索結果はドロップダウンで表示
      独立した画面遷移は不要（MVP）
```

### 13.3 機能一覧表（v3.12）との整合性

| 現在の定義 | 本設計との対応 |
|-----------|---------------|
| F-DGM-10 学習コンテンツ検索 | UC 3-10対応（Phase 2） |
| （未定義） | グローバル検索（MVP標準機能） |

**推奨**: 機能一覧表にF-CMN-01「グローバル検索」を追加検討

---

## 14. 関連ドキュメント

| ドキュメント | パス | 関連内容 |
|-------------|------|---------|
| TD-006 Storage設計 | `docs/context/technical_decisions.md` §TD-006 | データ保存方式 |
| TD-007 AI機能構成 | `docs/context/technical_decisions.md` §TD-007 | セマンティック検索 |
| UC図 | `docs/proposals/02_ユースケース図_20251130.md` | UC 3-10 |
| 機能一覧表 | `docs/proposals/05_機能一覧表_20251213.md` | F-DGM-10 |
| クラス図 | `docs/proposals/06_クラス図_20251208.md` | サービス層 |
| 画面遷移図 | `docs/proposals/diagrams/09_screen_transition/` | 画面フロー |
| ダッシュボードWF | `../02_dashboard/default.excalidraw` | 検索UI配置 |

---

## 15. 画面サイズ対応

### 15.1 検索バーのレスポンシブ設計

| ビューポート | 検索バー幅 | 配置 | スコープ選択 |
|-------------|-----------|------|-------------|
| ≥1920px | 400px | ヘッダー中央 | インライン |
| 1366-1919px | 300px | ヘッダー中央 | インライン |
| 1280-1365px | 280px | ヘッダー中央 | ドロップダウン |
| <1280px | アイコンのみ | クリックで展開 | ドロップダウン |

---

## 更新履歴

| 日付 | バージョン | 内容 |
|------|:----------:|------|
| 2025-12-27 | 1.0 | 初版作成 |
| 2025-12-27 | **2.0** | **MVP必須項目追加（結果ソート、件数表示、スコープ選択、タイプフィルタ、クリア機能、データローディング戦略）、エラー処理・パフォーマンス・i18n追加、コード品質改善、他設計整合性追加** |
